# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.Python

# Virtual environments
.venv/
.venv-notebooklm/
venv/
env/

# Secrets — DO NOT COMMIT
.env
.env.local
.env.*.local
*.key
*.pem
*.p12
*.pfx
credentials.json
token.json
client_secret*.json
service-account*.json
service_account*.json
secrets/
secrets.*
*.secret

# Auth / cookies / sessions
cookies.txt
*.cookies
.auth/
auth_state.json

# Cloud / infra credentials
.aws/
.gcloud/
.azure/
*.tfstate
*.tfstate.backup
.terraform/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude / agent local state
.claude/
.claude-*/

# Logs / temp
*.log
*.tmp
tmp/
temp/

# Test caches
.pytest_cache/
.coverage
htmlcov/

# TTS output — gitignore the ad-hoc one in cwd; commit the curated examples/outputs/*.mp3
/output.mp3
output.mp3

# SQLite trace DB (Tier 3 observability — local-only)
*.db
*.sqlite
*.sqlite3
traces.db-wal
traces.db-shm

# Notebook research artifacts (keep locally, not in submission)
# briefing-doc.md  # uncomment if you want to exclude
